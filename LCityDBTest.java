/**
*
* Tests DB operations implemented using a linked list
*
* Run with:
*    $ java LCityDBTest < input_file_name
*/

public class LCityDBTest {
   public static void main (String[] args) {
      LCityDB cityDB = new LCityDB(); // create a DB

      // For each line of the input file
      //   get city name, state, coordinates, and population
      //   using these data, insert a new record into DB

      // Do other operations on DB
   }
}
